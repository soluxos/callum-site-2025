// This layout intentionally bypasses the root layout's navigation, footer,
// and padding so the Ideas canvas can be truly full-screen.
// The <html>/<body> tags come from app/layout.js as usual.

export default function IdeasLayout({ children }) {
  return children;
}
